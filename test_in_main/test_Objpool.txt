const size_t len = 20;
	Test_b* arr[len];
	for (int i = 0; i < len; ++i) {
		arr[i] = Test_b::Create();
		arr[i]->set(i, i);
	}

	Test_b::pool.display();

	BLANK;

	arr[3]->Destory();
	Test_b::pool.display();

	BLANK;

	Test_b* obj1 = Test_b::Create();
	Test_b* obj2 = Test_b::Create();
	obj1->set(999, 99);
	obj2->set(888, 88);

	Test_b::pool.display();
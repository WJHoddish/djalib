djalib::Linkedlist<Test_a> mylist;
	djalib::Linkedlist<Test_a>::Iterator itr;

	Test_a obj_1(500);
	Test_a obj_2(800);
	Test_a obj_3(900);

	BLANK;

	mylist.push_back(obj_1);
	mylist.push_back(obj_2);
	mylist.push_back(obj_3);

	BLANK;

	for (itr = mylist.begin(); itr != mylist.end(); ++itr)
	{
		std::cout << *itr << std::endl;
	}

	BLANK;

	mylist.Remove(Test_a(400));

	BLANK;

	mylist.Destroy();